parameters:
  - name: modelName
    in: query
    type: string
    required: true
definitions:
  modelName:
    type: str
    example: "model_1"
responses:
  200:
    description: success
  400:
    description: Invalid model name.
schema:
  $ref: '#/definitions/modelName'